{
  "name": "package.json",
  "version": "1.0.0",
  "description": "scss ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass -w scss -o css --output-style compressed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.11.0"
  }
}
